name: Test Artifact

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: delete file
        run: |
          gh run artifact delete terraform-lock --repo teamg-hackathon-2025/VoucherZ
