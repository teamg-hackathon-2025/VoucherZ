name: Test Artifact

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: delete file
        run: |
          gh artifact delete terraform-tfstate --repo teamg-hackathon-2025/VoucherZ
